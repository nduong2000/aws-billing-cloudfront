name: Deploy Medical Billing Frontend to AWS CloudFront

on:
  push:
    branches: [ main, master ]

env:
  AWS_REGION: us-east-1
  S3_BUCKET: medical-billing-frontend-${{ github.ref_name }}-${{ github.run_number }}
  CLOUDFRONT_DISTRIBUTION_ID: ${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run tests (if any)
      run: npm test --if-present

    - name: Build application
      run: npm run build

    - name: Configure AWS credentials (alternative method)
      run: |
        echo "Setting up AWS credentials via environment variables..."
        mkdir -p ~/.aws
        cat > ~/.aws/credentials << EOF
        [default]
        aws_access_key_id = ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws_secret_access_key = ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        EOF
        cat > ~/.aws/config << EOF
        [default]
        region = ${{ env.AWS_REGION }}
        output = json
        EOF
        chmod 600 ~/.aws/credentials ~/.aws/config

    - name: Deep debug AWS credentials
      run: |
        echo "=== AWS Credential Debug ==="
        echo "Access Key ID length: ${#AWS_ACCESS_KEY_ID}"
        echo "Secret Key length: ${#AWS_SECRET_ACCESS_KEY}"
        echo "Access Key starts with: ${AWS_ACCESS_KEY_ID:0:4}"
        echo "Access Key ends with: ${AWS_ACCESS_KEY_ID: -4}"
        echo "Access Key ID (hex dump first 20 chars):"
        echo "$AWS_ACCESS_KEY_ID" | head -c 20 | xxd
        echo "=== Testing different regions ==="
        AWS_DEFAULT_REGION=us-east-1 aws sts get-caller-identity || echo "us-east-1 failed"
        AWS_DEFAULT_REGION=us-west-2 aws sts get-caller-identity || echo "us-west-2 failed"
        echo "=== AWS CLI version ==="
        aws --version
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

    - name: Test AWS credentials
      run: |
        echo "Testing AWS credentials..."
        aws sts get-caller-identity
        echo "AWS credentials working! ✅"

    - name: Create and configure S3 bucket
      run: |
        echo "Creating S3 bucket: ${{ env.S3_BUCKET }}"
        
        # Create bucket if it doesn't exist
        aws s3 mb s3://${{ env.S3_BUCKET }} --region ${{ env.AWS_REGION }} || true
        
        # Wait a moment for bucket creation to propagate
        sleep 5
        
        # Configure bucket for static website hosting
        aws s3 website s3://${{ env.S3_BUCKET }} \
          --index-document index.html \
          --error-document index.html
        
        # Set public access block configuration
        aws s3api put-public-access-block \
          --bucket ${{ env.S3_BUCKET }} \
          --public-access-block-configuration "BlockPublicAcls=false,IgnorePublicAcls=false,BlockPublicPolicy=false,RestrictPublicBuckets=false"
        
        # Create and apply bucket policy for public read access
        cat > bucket-policy.json << EOF
        {
            "Version": "2012-10-17",
            "Statement": [
                {
                    "Sid": "PublicReadGetObject",
                    "Effect": "Allow",
                    "Principal": "*",
                    "Action": "s3:GetObject",
                    "Resource": "arn:aws:s3:::${{ env.S3_BUCKET }}/*"
                }
            ]
        }
        EOF
        
        # Apply bucket policy
        aws s3api put-bucket-policy --bucket ${{ env.S3_BUCKET }} --policy file://bucket-policy.json
        
        echo "S3 bucket configured successfully! ✅"

    - name: Deploy to S3
      run: |
        echo "Deploying files to S3..."
        
        # Sync static assets with long cache
        aws s3 sync dist/ s3://${{ env.S3_BUCKET }} \
          --delete \
          --cache-control "max-age=31536000,public,immutable" \
          --exclude "*.html" \
          --exclude "*.json"
        
        # Sync HTML files with no cache
        aws s3 sync dist/ s3://${{ env.S3_BUCKET }} \
          --delete \
          --cache-control "max-age=0,no-cache,no-store,must-revalidate" \
          --include "*.html"
        
        # Sync manifest and config files with short cache
        aws s3 sync dist/ s3://${{ env.S3_BUCKET }} \
          --delete \
          --cache-control "max-age=300,public" \
          --include "*.json"
        
        echo "Files deployed to S3 successfully! ✅"

    - name: Invalidate CloudFront cache
      if: env.CLOUDFRONT_DISTRIBUTION_ID != ''
      run: |
        echo "Invalidating CloudFront cache..."
        aws cloudfront create-invalidation \
          --distribution-id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} \
          --paths "/*"
        echo "CloudFront cache invalidated! ✅"

    - name: Deploy summary
      run: |
        echo ""
        echo "🎉 Deployment completed successfully!"
        echo ""
        echo "📦 S3 Bucket: ${{ env.S3_BUCKET }}"
        echo "📱 S3 Website: http://${{ env.S3_BUCKET }}.s3-website-${{ env.AWS_REGION }}.amazonaws.com"
        echo "🌍 Region: ${{ env.AWS_REGION }}"
        echo ""
        if [ -n "${{ env.CLOUDFRONT_DISTRIBUTION_ID }}" ]; then
          CLOUDFRONT_DOMAIN=$(aws cloudfront get-distribution --id ${{ env.CLOUDFRONT_DISTRIBUTION_ID }} --query 'Distribution.DomainName' --output text)
          echo "🌐 CloudFront: https://$CLOUDFRONT_DOMAIN"
          echo ""
        fi
        echo "✨ Your Medical Billing Frontend is now live!! ✨"